.container
  .row
    .col-sm-6.col-sm-offset-3
      .hero-unit
        %h1 Asset Crawler

        = simple_form_for @domain, class: 'form' do |f|
          %p.lead Enter a domain and start crawling!

          .form-group
            = f.input :url, input_html: { class: 'form-control',
              placeholder: 'http://www.example.com' }

          = f.submit 'Crawl!', class: 'btn btn-lg btn-warning'
